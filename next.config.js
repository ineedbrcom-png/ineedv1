
/** @type {import('next').NextConfig} */
const nextConfig = {
  // A opção 'experimental.serverActions' foi removida.
  // Server Actions são habilitadas por padrão nas versões recentes do Next.js.
};

module.exports = nextConfig;
